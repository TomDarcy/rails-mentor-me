<div class="mentor-hero">
  <div class="top-right">
    <% if user_signed_in? == false %>
      <%= link_to "Login", new_user_session_path, class: "login-btn-small" %>
    <% else %>
      <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "login-btn-small"
 %>
    <% end %>
  </div>
  <div class="top-left">
    <%= image_tag "logonb.png", class: "top-left" %>
  </div>
  <% if user_signed_in? %>
    <div class="landing-header">
      Mentor Me <br>
      <div class="smaller-header">      Welcome back <%= current_user.first_name.capitalize %>!</div>
    </div>
  <% else %>
    <div class="landing-header">
      Start your journey with MentorMe
    </div>
  <% end %>
  <div class="button-box">
    <%= link_to "Find your mentor", mentors_path, class: "btn-flat-home" %>
    <% if user_signed_in? && current_user.mentor %>
      <% mentor = Mentor.find_by_user_id(current_user.id) %>
      <%= link_to "Manage my Profile", edit_mentor_path((mentor)), class: "btn-ghost" %>
    <% else %>
      <%= link_to "Become a mentor", new_user_registration_path, class: "btn-ghost" %>
    <% end %>
  </div>
  <div class="quote">
    <em class="quote">"Great mentors help us to see beyond our own limitations and unlock our potential to become better developers." <br>
      Addy Osmani, Engineering Manager at Google and Web Development Advocate.</em>
  </div>
</div>
